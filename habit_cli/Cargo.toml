[package]
name = "habit_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.7", features = ["derive"] }
habit_core = {path = "../habit_core"}
habit_storage = {path = "../habit_storage"}
